import 'package:flutter/material.dart';

class PhimBoTab extends StatelessWidget {
  const PhimBoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Phim bộ"));
  }
}
